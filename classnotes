07/15/2023
-----------

Connected to google VM
Created instance docker (docker is the name of the instance)
Added ubuntu server
use SSH connect
to know the hostname  type
   hostname -i

to change to root use the command sudo -i
